// Code generated by goctl. DO NOT EDIT.
// versions:
//  goctl version: 1.7.3

package model

import (
	"context"
	"database/sql"
	"fmt"
	"strings"
	"time"

	"github.com/zeromicro/go-zero/core/stores/builder"
	"github.com/zeromicro/go-zero/core/stores/cache"
	"github.com/zeromicro/go-zero/core/stores/sqlc"
	"github.com/zeromicro/go-zero/core/stores/sqlx"
	"github.com/zeromicro/go-zero/core/stringx"
)

var (
	onecResourceLabelsFieldNames          = builder.RawFieldNames(&OnecResourceLabels{})
	onecResourceLabelsRows                = strings.Join(onecResourceLabelsFieldNames, ",")
	onecResourceLabelsRowsExpectAutoSet   = strings.Join(stringx.Remove(onecResourceLabelsFieldNames, "`id`", "`create_at`", "`create_time`", "`created_at`", "`update_at`", "`update_time`", "`updated_at`"), ",")
	onecResourceLabelsRowsWithPlaceHolder = strings.Join(stringx.Remove(onecResourceLabelsFieldNames, "`id`", "`create_at`", "`create_time`", "`created_at`", "`update_at`", "`update_time`", "`updated_at`"), "=?,") + "=?"

	cacheKubeOnecOnecResourceLabelsIdPrefix                        = "cache:kubeOnec:onecResourceLabels:id:"
	cacheKubeOnecOnecResourceLabelsResourceTypeResourceIdKeyPrefix = "cache:kubeOnec:onecResourceLabels:resourceType:resourceId:key:"
)

type (
	onecResourceLabelsModel interface {
		Insert(ctx context.Context, data *OnecResourceLabels) (sql.Result, error)

		FindOne(ctx context.Context, id uint64) (*OnecResourceLabels, error)
		Search(ctx context.Context, orderStr string, isAsc bool, page, pageSize uint64, queryStr string, args ...any) ([]*OnecResourceLabels, uint64, error)
		SearchNoPage(ctx context.Context, orderStr string, isAsc bool, queryStr string, args ...any) ([]*OnecResourceLabels, error)
		FindOneByResourceTypeResourceIdKey(ctx context.Context, resourceType string, resourceId uint64, key string) (*OnecResourceLabels, error)
		Update(ctx context.Context, data *OnecResourceLabels) error
		Delete(ctx context.Context, id uint64) error
		DeleteSoft(ctx context.Context, id uint64) error
		TransCtx(ctx context.Context, fn func(context.Context, sqlx.Session) error) error
		TransOnSql(ctx context.Context, session sqlx.Session, id uint64, sqlStr string, args ...any) (sql.Result, error)
		ExecSql(ctx context.Context, id uint64, sqlStr string, args ...any) (sql.Result, error)
	}

	defaultOnecResourceLabelsModel struct {
		sqlc.CachedConn
		table string
	}

	OnecResourceLabels struct {
		Id           uint64    `db:"id"`            // 自增主键
		ResourceType string    `db:"resource_type"` // 资源类型，例如 node, pod
		ResourceId   uint64    `db:"resource_id"`   // 关联的资源ID
		Key          string    `db:"key"`           // 标签键
		Value        string    `db:"value"`         // 标签值
		CreatedAt    time.Time `db:"created_at"`    // 记录创建时间
		UpdatedAt    time.Time `db:"updated_at"`    // 记录更新时间
		IsDeleted    int64     `db:"is_deleted"`    // 是否删除
	}
)

func newOnecResourceLabelsModel(conn sqlx.SqlConn, c cache.CacheConf, opts ...cache.Option) *defaultOnecResourceLabelsModel {
	return &defaultOnecResourceLabelsModel{
		CachedConn: sqlc.NewConn(conn, c, opts...),
		table:      "`onec_resource_labels`",
	}
}

func (m *defaultOnecResourceLabelsModel) Delete(ctx context.Context, id uint64) error {
	data, err := m.FindOne(ctx, id)
	if err != nil {
		return err
	}

	kubeOnecOnecResourceLabelsIdKey := fmt.Sprintf("%s%v", cacheKubeOnecOnecResourceLabelsIdPrefix, id)
	kubeOnecOnecResourceLabelsResourceTypeResourceIdKeyKey := fmt.Sprintf("%s%v:%v:%v", cacheKubeOnecOnecResourceLabelsResourceTypeResourceIdKeyPrefix, data.ResourceType, data.ResourceId, data.Key)
	_, err = m.ExecCtx(ctx, func(ctx context.Context, conn sqlx.SqlConn) (result sql.Result, err error) {
		query := fmt.Sprintf("delete from %s where `id` = ?", m.table)
		return conn.ExecCtx(ctx, query, id)
	}, kubeOnecOnecResourceLabelsIdKey, kubeOnecOnecResourceLabelsResourceTypeResourceIdKeyKey)
	return err
}

func (m *defaultOnecResourceLabelsModel) DeleteSoft(ctx context.Context, id uint64) error {
	data, err := m.FindOne(ctx, id)
	if err != nil {
		return err
	}
	// 如果记录已软删除，无需再次删除
	if data.IsDeleted == 1 {
		return nil
	}
	kubeOnecOnecResourceLabelsIdKey := fmt.Sprintf("%s%v", cacheKubeOnecOnecResourceLabelsIdPrefix, id)
	kubeOnecOnecResourceLabelsResourceTypeResourceIdKeyKey := fmt.Sprintf("%s%v:%v:%v", cacheKubeOnecOnecResourceLabelsResourceTypeResourceIdKeyPrefix, data.ResourceType, data.ResourceId, data.Key)
	_, err = m.ExecCtx(ctx, func(ctx context.Context, conn sqlx.SqlConn) (result sql.Result, err error) {
		query := fmt.Sprintf("update %s set `is_deleted` = 1 where `id` = ?", m.table)
		return conn.ExecCtx(ctx, query, id)
	}, kubeOnecOnecResourceLabelsIdKey, kubeOnecOnecResourceLabelsResourceTypeResourceIdKeyKey)
	return err
}

func (m *defaultOnecResourceLabelsModel) TransCtx(ctx context.Context, fn func(context.Context, sqlx.Session) error) error {
	return m.TransactCtx(ctx, func(ctx context.Context, session sqlx.Session) error {
		return fn(ctx, session)
	})
}

func (m *defaultOnecResourceLabelsModel) TransOnSql(ctx context.Context, session sqlx.Session, id uint64, sqlStr string, args ...any) (sql.Result, error) {
	query := strings.ReplaceAll(sqlStr, "{table}", m.table)
	// 如果 id != 0 并且启用了缓存逻辑
	if !isZeroValue(id) {
		// 查询数据（如果需要，确保数据存在）
		data, err := m.FindOne(ctx, id)
		if err != nil {
			return nil, err
		}

		// 缓存相关处理

		kubeOnecOnecResourceLabelsIdKey := fmt.Sprintf("%s%v", cacheKubeOnecOnecResourceLabelsIdPrefix, id)
		kubeOnecOnecResourceLabelsResourceTypeResourceIdKeyKey := fmt.Sprintf("%s%v:%v:%v", cacheKubeOnecOnecResourceLabelsResourceTypeResourceIdKeyPrefix, data.ResourceType, data.ResourceId, data.Key) // 处理缓存逻辑，例如删除或更新缓存
		// 执行带缓存处理的 SQL 操作
		return m.ExecCtx(ctx, func(ctx context.Context, conn sqlx.SqlConn) (sql.Result, error) {
			return session.ExecCtx(ctx, query, args...)
		}, kubeOnecOnecResourceLabelsIdKey, kubeOnecOnecResourceLabelsResourceTypeResourceIdKeyKey) // 传递缓存相关的键值

	}

	// 如果 id == 0 或不需要缓存，直接执行 SQL
	return m.ExecCtx(ctx, func(ctx context.Context, conn sqlx.SqlConn) (sql.Result, error) {
		return session.ExecCtx(ctx, query, args...)
	})
}

func (m *defaultOnecResourceLabelsModel) ExecSql(ctx context.Context, id uint64, sqlStr string, args ...any) (sql.Result, error) {
	// 如果 id != 0 并且启用了缓存逻辑
	query := strings.ReplaceAll(sqlStr, "{table}", m.table)
	if !isZeroValue(id) {
		// 缓存相关处理

		// 查询数据（如果需要，确保数据存在）
		data, err := m.FindOne(ctx, id)
		if err != nil {
			return nil, err
		}

		kubeOnecOnecResourceLabelsIdKey := fmt.Sprintf("%s%v", cacheKubeOnecOnecResourceLabelsIdPrefix, id)
		kubeOnecOnecResourceLabelsResourceTypeResourceIdKeyKey := fmt.Sprintf("%s%v:%v:%v", cacheKubeOnecOnecResourceLabelsResourceTypeResourceIdKeyPrefix, data.ResourceType, data.ResourceId, data.Key) // 处理缓存逻辑，例如删除或更新缓存
		// 执行带缓存处理的 SQL 操作
		return m.ExecCtx(ctx, func(ctx context.Context, conn sqlx.SqlConn) (sql.Result, error) {
			return conn.ExecCtx(ctx, query, args...)
		}, kubeOnecOnecResourceLabelsIdKey, kubeOnecOnecResourceLabelsResourceTypeResourceIdKeyKey) // 传递缓存相关的键值

	}

	// 如果 id == 0 或不需要缓存，直接执行 SQL
	return m.ExecCtx(ctx, func(ctx context.Context, conn sqlx.SqlConn) (sql.Result, error) {
		return conn.ExecCtx(ctx, query, args...)
	})
}
func (m *defaultOnecResourceLabelsModel) FindOne(ctx context.Context, id uint64) (*OnecResourceLabels, error) {
	kubeOnecOnecResourceLabelsIdKey := fmt.Sprintf("%s%v", cacheKubeOnecOnecResourceLabelsIdPrefix, id)
	var resp OnecResourceLabels
	err := m.QueryRowCtx(ctx, &resp, kubeOnecOnecResourceLabelsIdKey, func(ctx context.Context, conn sqlx.SqlConn, v any) error {
		query := fmt.Sprintf("select %s from %s where `id` = ? AND `is_deleted` = 0 limit 1", onecResourceLabelsRows, m.table)
		return conn.QueryRowCtx(ctx, v, query, id)
	})
	switch err {
	case nil:
		return &resp, nil
	case sqlc.ErrNotFound:
		return nil, ErrNotFound
	default:
		return nil, err
	}
}

func (m *defaultOnecResourceLabelsModel) Search(ctx context.Context, orderStr string, isAsc bool, page, pageSize uint64, queryStr string, args ...any) ([]*OnecResourceLabels, uint64, error) {
	// 确保分页参数有效
	if page < 1 {
		page = 1
	}
	if pageSize < 1 {
		pageSize = 20
	}

	// 构造查询条件
	// 添加 `is_deleted` = 0 条件，保证只查询未软删除数据
	// 初始化 WHERE 子句
	where := "WHERE `is_deleted` = 0"
	if queryStr != "" {
		where = fmt.Sprintf("WHERE %s AND `is_deleted` = 0", queryStr)
	}

	// 根据 isAsc 参数确定排序方式
	sortDirection := "ASC"
	if !isAsc {
		sortDirection = "DESC"
	}

	// 如果用户未指定排序字段，则默认使用 id
	if orderStr == "" {
		orderStr = fmt.Sprintf("ORDER BY id %s", sortDirection)
	} else {
		orderStr = strings.TrimSpace(orderStr)
		if !strings.HasPrefix(strings.ToUpper(orderStr), "ORDER BY") {
			orderStr = "ORDER BY " + orderStr
		}
		orderStr = fmt.Sprintf("%s %s", orderStr, sortDirection)
	}

	countQuery := fmt.Sprintf("SELECT COUNT(1) FROM %s %s", m.table, where)

	var total uint64
	var resp []*OnecResourceLabels
	err := m.QueryRowNoCacheCtx(ctx, &total, countQuery, args...)
	if err != nil {
		return nil, 0, err
	}
	if total == 0 {
		// 无匹配记录
		return resp, 0, ErrNotFound
	}
	offset := (page - 1) * pageSize
	dataQuery := fmt.Sprintf("SELECT %s FROM %s %s %s LIMIT %d,%d", onecResourceLabelsRows, m.table, where, orderStr, offset, pageSize)

	err = m.QueryRowsNoCacheCtx(ctx, &resp, dataQuery, args...)
	if err != nil {
		return nil, 0, err
	}

	return resp, total, nil
}

func (m *defaultOnecResourceLabelsModel) SearchNoPage(ctx context.Context, orderStr string, isAsc bool, queryStr string, args ...any) ([]*OnecResourceLabels, error) {
	// 初始化 WHERE 子句
	where := "WHERE `is_deleted` = 0"
	if queryStr != "" {
		where = fmt.Sprintf("WHERE %s AND `is_deleted` = 0", queryStr)
	}

	// 根据 isAsc 参数确定排序方式
	sortDirection := "ASC"
	if !isAsc {
		sortDirection = "DESC"
	}
	// 如果用户未指定排序字段，则默认使用 id
	if orderStr == "" {
		orderStr = fmt.Sprintf("ORDER BY id %s", sortDirection)
	} else {
		orderStr = strings.TrimSpace(orderStr)
		if !strings.HasPrefix(strings.ToUpper(orderStr), "ORDER BY") {
			orderStr = "ORDER BY " + orderStr
		}
		orderStr = fmt.Sprintf("%s %s", orderStr, sortDirection)
	}
	dataQuery := fmt.Sprintf("SELECT %s FROM %s %s %s", onecResourceLabelsRows, m.table, where, orderStr)
	var resp []*OnecResourceLabels
	err := m.QueryRowsNoCacheCtx(ctx, &resp, dataQuery, args...)
	switch err {
	case nil:
		return resp, nil
	case sqlx.ErrNotFound:
		return nil, ErrNotFound
	default:
		return nil, err
	}
}
func (m *defaultOnecResourceLabelsModel) FindOneByResourceTypeResourceIdKey(ctx context.Context, resourceType string, resourceId uint64, key string) (*OnecResourceLabels, error) {
	kubeOnecOnecResourceLabelsResourceTypeResourceIdKeyKey := fmt.Sprintf("%s%v:%v:%v", cacheKubeOnecOnecResourceLabelsResourceTypeResourceIdKeyPrefix, resourceType, resourceId, key)
	var resp OnecResourceLabels
	err := m.QueryRowIndexCtx(ctx, &resp, kubeOnecOnecResourceLabelsResourceTypeResourceIdKeyKey, m.formatPrimary, func(ctx context.Context, conn sqlx.SqlConn, v any) (i any, e error) {
		query := fmt.Sprintf("select %s from %s where `resource_type` = ? and `resource_id` = ? and `key` = ? AND `is_deleted` = 0  limit 1", onecResourceLabelsRows, m.table)
		if err := conn.QueryRowCtx(ctx, &resp, query, resourceType, resourceId, key); err != nil {
			return nil, err
		}
		return resp.Id, nil
	}, m.queryPrimary)
	switch err {
	case nil:
		return &resp, nil
	case sqlc.ErrNotFound:
		return nil, ErrNotFound
	default:
		return nil, err
	}
}

func (m *defaultOnecResourceLabelsModel) Insert(ctx context.Context, data *OnecResourceLabels) (sql.Result, error) {
	kubeOnecOnecResourceLabelsIdKey := fmt.Sprintf("%s%v", cacheKubeOnecOnecResourceLabelsIdPrefix, data.Id)
	kubeOnecOnecResourceLabelsResourceTypeResourceIdKeyKey := fmt.Sprintf("%s%v:%v:%v", cacheKubeOnecOnecResourceLabelsResourceTypeResourceIdKeyPrefix, data.ResourceType, data.ResourceId, data.Key)
	ret, err := m.ExecCtx(ctx, func(ctx context.Context, conn sqlx.SqlConn) (result sql.Result, err error) {
		query := fmt.Sprintf("insert into %s (%s) values (?, ?, ?, ?, ?)", m.table, onecResourceLabelsRowsExpectAutoSet)
		return conn.ExecCtx(ctx, query, data.ResourceType, data.ResourceId, data.Key, data.Value, data.IsDeleted)
	}, kubeOnecOnecResourceLabelsIdKey, kubeOnecOnecResourceLabelsResourceTypeResourceIdKeyKey)
	return ret, err
}

func (m *defaultOnecResourceLabelsModel) Update(ctx context.Context, newData *OnecResourceLabels) error {
	data, err := m.FindOne(ctx, newData.Id)
	if err != nil {
		return err
	}

	kubeOnecOnecResourceLabelsIdKey := fmt.Sprintf("%s%v", cacheKubeOnecOnecResourceLabelsIdPrefix, data.Id)
	kubeOnecOnecResourceLabelsResourceTypeResourceIdKeyKey := fmt.Sprintf("%s%v:%v:%v", cacheKubeOnecOnecResourceLabelsResourceTypeResourceIdKeyPrefix, data.ResourceType, data.ResourceId, data.Key)
	_, err = m.ExecCtx(ctx, func(ctx context.Context, conn sqlx.SqlConn) (result sql.Result, err error) {
		query := fmt.Sprintf("update %s set %s where `id` = ?", m.table, onecResourceLabelsRowsWithPlaceHolder)
		return conn.ExecCtx(ctx, query, newData.ResourceType, newData.ResourceId, newData.Key, newData.Value, newData.IsDeleted, newData.Id)
	}, kubeOnecOnecResourceLabelsIdKey, kubeOnecOnecResourceLabelsResourceTypeResourceIdKeyKey)
	return err
}

func (m *defaultOnecResourceLabelsModel) formatPrimary(primary any) string {
	return fmt.Sprintf("%s%v", cacheKubeOnecOnecResourceLabelsIdPrefix, primary)
}

func (m *defaultOnecResourceLabelsModel) queryPrimary(ctx context.Context, conn sqlx.SqlConn, v, primary any) error {
	query := fmt.Sprintf("select %s from %s where `id` = ? AND `is_deleted` = 0 limit 1", onecResourceLabelsRows, m.table)
	return conn.QueryRowCtx(ctx, v, query, primary)
}

func (m *defaultOnecResourceLabelsModel) tableName() string {
	return m.table
}
